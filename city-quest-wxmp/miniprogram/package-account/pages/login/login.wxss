.page { padding: 32rpx; background: var(--color-paper); color: var(--color-ink); min-height: 100vh; }
.title { font-size: 34rpx; font-weight: 600; margin-bottom: 16rpx; }
.hint { font-size: 24rpx; color: var(--color-ink-muted); line-height: 1.5; }
