<view class="page">
  <view class="title">登录</view>
  <view class="hint">骨架：完整头像昵称协议见 PRD。开发可用服务端 dev code。</view>
  <button loading="{{loading}}" bindtap="onLogin">微信登录</button>
  <view class="error" wx:if="{{error}}">{{error}}</view>
</view>
