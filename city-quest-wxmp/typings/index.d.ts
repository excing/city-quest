/// <reference path="./types/index.d.ts" />

/**
 * Callers: app.ts App generic. Schema: globalData bag for stage A.
 * User: 阅读 @docs , 然后选择合适的agents或skills, 开始进行开发.
 */
interface IAppOption {
  globalData: Record<string, unknown>
}
